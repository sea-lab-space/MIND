# 🧠 MH Narrative Dashboard

Backend for clinician-facing mental health dashboard.

---

## Installation

`pip install uv`

Check if uv is installed
`uv --version`

Now the uv is installed in this specific conda environment.

Sync dependencies:
`uv sync`

Remember to add packages using uv (if not possible, use uv pip).